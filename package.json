{
  "name": "stellar99-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint",
    "contract:test": "cargo test --manifest-path contracts/lumensflow-stream/Cargo.toml"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "@clerk/express": "^2.1.9",
    "@stellar/stellar-sdk": "^15.0.1",
    "stellar-sdk": "^11.2.2"
  }
}
